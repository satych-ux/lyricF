import 'package:flutter/material.dart';

/// [AppColors] defines the brand color palette for the application.
/// Using a dedicated class ensures consistency across the app.
class AppColors {
  // Brand Colors
  static const Color primary = Color(0xFFF4A825); // Saffron Gold
  
  // Light Theme
  static const Color backgroundLight = Color(0xFFF8F7F5);
  static const Color surfaceLight = Color(0xFFFFFFFF);
  static const Color textMainLight = Color(0xFF181511);
  static const Color textSecondaryLight = Color(0xFF8B7E6A);
  
  // Dark Theme
  static const Color backgroundDark = Color(0xFF221B10);
  static const Color surfaceDark = Color(0xFF2D2818);
  static const Color textMainDark = Color(0xFFF8F5E6);
  static const Color textSecondaryDark = Color(0xFFD1CBB0);
}
